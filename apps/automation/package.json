{
  "name": "@qa/automation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:headed": "playwright test --grep @smoke --headed",
    "lint": "eslint . --ext .js,.ts --no-error-on-unmatched-pattern",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.0.0"
  }
}
