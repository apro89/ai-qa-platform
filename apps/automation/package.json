{
  "name": "@qa/automation", "version": "0.1.0", "private": true,
  "scripts": { "test": "playwright test", "lint": "eslint . --ext .js,.ts,.tsx --no-error-on-unmatched-pattern", "build": "tsc --noEmit" },
  "devDependencies": { "@playwright/test": "^1.50.0", "typescript": "^5.0.0" }
}
