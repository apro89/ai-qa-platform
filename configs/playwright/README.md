# Playwright configuration

Holds organization-wide browser projects, retries, trace/video retention, reporters, and environment-safe defaults. Application-specific settings remain in `apps/automation/config`.
